
import { createRoot } from "react-dom/client";
import App from "./app/App";
import "./styles/index.css";

// Monte l'application React dans l'element racine de la page.
createRoot(document.getElementById("root")!).render(<App />);
  
